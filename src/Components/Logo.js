import React from 'react';

export default function Logo({ width, height }) {
  return (
    <img
      src="/images/logo.png"
      alt="serviceLogo"
      width={width}
      height={height}
    />
  );
}
